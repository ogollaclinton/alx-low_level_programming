#include "main.h"
/**
 *_abs - 'Compute absolute value of an integer'
 * @p: int to check
 * Return: Absolute value of integer
 */


int _abs(int p)
{
	if (p >= 0)
	{
		return (p);
	}
	else
	{
		return (-p);
	}
}
